/**
 * Type script utilizando arrow function (ES6) e
 * tipagem de dados (TS).
 * 
 * Esse é o resultado do transpile.
 */
var ola = function (nome) {
    console.log('Olá ' + nome);
};
ola('Ricardo');
